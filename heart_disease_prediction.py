# -*- coding: utf-8 -*-
"""Heart Disease Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vMLLtsYN0we2Fo5Lo8A_1mFL01pIoi1j
"""

import numpy as np
import pandas as pd
from sklearn.svm import SVC
from sklearn.model_selection import train_test_split
from sklearn.metrics import classification_report

df = pd.read_csv('/content/heart.csv')
df

print(df['target'].unique())

df.columns

df.isnull().sum()

x = df.drop(columns='target',axis=1)
y = df['target']
df['target'].value_counts()

x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.2, stratify=y, random_state=2)

clf1 = SVC(kernel="linear")
clf1.fit(x_train,y_train)

y_pred= clf1.predict(x_test)
classification_rep = classification_report(y_pred,y_test)
print("Classification Report:")
print(classification_rep)

def heart(model, input_data):
    prediction = model.predict(input_data)
    if prediction[0] == 0:
        print("The Person does not have Heart Disease 💚")
    else:
        print("The Person has Heart Disease ❤️")

# Example
input_data = pd.DataFrame([[58, 5, 34, 130, 56, 4, 0, 256, 0, 6, 0, 2.7, 10]],columns=x_train.columns)
heart(clf1, input_data)